add_library(dmrg-math-linalg OBJECT)
target_sources(dmrg-math-linalg PRIVATE matrix.cpp)
target_link_libraries(dmrg-math-linalg PUBLIC dmrg-deps dmrg-flags)
if (DMRG_ENABLE_PCH)
    target_precompile_headers(dmrg-math-linalg REUSE_FROM dmrg-pch)
endif ()