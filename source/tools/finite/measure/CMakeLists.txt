add_library(dmrg-tools-finite-measure OBJECT)
target_sources(dmrg-tools-finite-measure PRIVATE measure.cpp entropy.cpp)
target_link_libraries(dmrg-tools-finite-measure PUBLIC dmrg-deps dmrg-flags)
if (DMRG_ENABLE_PCH)
    target_precompile_headers(dmrg-tools-finite-measure REUSE_FROM dmrg-pch)
endif ()