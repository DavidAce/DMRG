add_library(dmrg-tools-infinite-measure OBJECT)
target_sources(dmrg-tools-infinite-measure PRIVATE measure.cpp measure_ham.cpp measure_mom.cpp)
target_link_libraries(dmrg-tools-infinite-measure PUBLIC dmrg-deps dmrg-flags)
if (DMRG_ENABLE_PCH)
    target_precompile_headers(dmrg-tools-infinite-measure REUSE_FROM dmrg-pch)
endif ()