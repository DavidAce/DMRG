add_library(dmrg-tools-infinite-mps OBJECT)
target_sources(dmrg-tools-infinite-mps PRIVATE mps.cpp)
target_link_libraries(dmrg-tools-infinite-mps PUBLIC dmrg-deps dmrg-flags)
if (DMRG_ENABLE_PCH)
    target_precompile_headers(dmrg-tools-infinite-mps REUSE_FROM dmrg-pch)
endif ()